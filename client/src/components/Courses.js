import React from 'react'
import studentData from "../data/studentData"

function Courses() {
    return (
        <div>
            {studentData.courseInformation?.forEach((el,index)=>{
                
            })

            }
            
        </div>
    )
}

export default Courses
